#include <stdio.h>
#include <math.h>

int main()

{
    const double a=cos(2);
    double x;
    const double c=M_PI/6;
    const double d=sqrt(2);
    const double f=1/log2(x);
    const double g=pow(sin(pow(x,2)),2);
    const double q=exp(3);
    double y;

    printf("input x=");
    y=((2*(cos(x-(M_PI/6))+sqrt(2)))/((1/(2*log(x)))+pow(sin(pow(x,2)),2)))*exp(3*x);
    scanf("%lf",&x,a,c,d,f,g,q,y);

    return 0;
}

